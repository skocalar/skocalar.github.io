// You can use JavaScript to dynamically change the source or position of the overlay image
// For example, you can use the following code to move the image up or down when a button is clicked:
var overlayImage = document.getElementById("overlay-image");
overlayImage.style.transform = "translateY(-50px)"; // move the image up by 50 pixels
